# 🚀 دليل النشر - Bookfolloxa Bot

## ⚠️ مهم جداً: كيفية النشر الصحيح

### المشكلة الشائعة
❌ البوت يستخدم **polling mode** ولا يمكن تشغيله في مكانين في نفس الوقت!

### ✅ الحل الصحيح

#### خيار 1: النشر على Reserved VM (موصى به)

1. **أوقف البوت في Development:**
   - اضغط على زر **Stop** في console
   - أو أعد تشغيل Repl بدون تشغيل البوت

2. **انشر البوت:**
   - اضغط زر **Deploy** في Replit
   - تأكد من:
     - ✅ Deployment Type = **Reserved VM** (محدد في .replit)
     - ✅ Run command = `python main.py`
   - اضغط **Deploy**

3. **البوت سيعمل 24/7:**
   - متصل بـ PostgreSQL
   - يستقبل الرسائل
   - WebApp شغالة

#### خيار 2: للتطوير والتجربة فقط

```bash
# شغّل البوت محلياً للتجربة:
python main.py
```

**⚠️ لا تنشر (Deploy) والبوت شغال محلياً!**

---

## 🎮 رابط اللعبة (WebApp)

اللعبة تعمل حتى بدون البوت:
```
https://[YOUR-REPL-URL]/webapp/
```

---

## 🔧 إعدادات الـ Deployment

تم إعداد الملف `.replit` بالشكل الصحيح:

```toml
[deployment]
deploymentTarget = "vm"    # Reserved VM (ليس autoscale)
run = ["python", "main.py"]
```

---

## ❓ الأسئلة الشائعة

**س: لماذا يظهر خطأ "Multiple bot instances"؟**
ج: لأن البوت يعمل في development و deployment في نفس الوقت. أوقف واحد منهم.

**س: هل يمكن استخدام Autoscale؟**
ج: لا مع polling mode. يجب استخدام Reserved VM أو تحويل البوت لـ webhooks.

**س: كيف أعرف إذا البوت شغال؟**
ج: افتح البوت في تيليجرام واكتب `/start`. إذا رد = شغال ✅

---

## 📞 الدعم

إذا واجهت مشاكل، تأكد من:
1. ✅ البوت متوقف في development
2. ✅ Deployment type = Reserved VM
3. ✅ TELEGRAM_BOT_TOKEN موجود في Secrets
4. ✅ PostgreSQL database متصلة
